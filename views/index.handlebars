<main class='container'>

    {{#each articles}}
    <div class="card blue-grey darken-1">
        <div class="card-content white-text">
            <span class="card-title"></span>
            <div id='articles'>
                <p class="articles">{{title}}</p>
                    <br><a href="https://www.orlandosentinel.com{{link}}">{{title}}</a>
            </div>
        </div>
    </div>
    {{/each}}
    
     {{!--
    <div id='articles'>
        {{#each articles}}
        <p class="articles">{{title}}
            <br>{{link}}</p>
        {{/each}}
    </div>

    <div id='notes'>
        {{#each notes}}
        <p class="notes">{{note}}</p>
        {{/each}}
    </div> --}}

</main>